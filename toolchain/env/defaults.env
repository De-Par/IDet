# Repository defaults
# -----------------------------------------------------------
# Layering order:
#   1) toolchain/env/defaults.env          (repo, committed)
#   2) toolchain/profiles/<P>.ini           (repo, committed)
#   3) toolchain/env/local.env             (optional local/repo overrides)
#   4) explicit env vars in calling shell  (CI/job overrides)
# -----------------------------------------------------------

# Default profile
TC_PROFILE=gcc-perf

# Build 
BUILD_DIR=build
MESON=meson
NINJA=ninja
PKG_CONFIG=pkg-config 

# Policy flags
NONINTERACTIVE=1
KEEP_SUBPROJECTS=1

# Threads (auto)
JOBS=0

# LLVM / Clang 
LLVM_VER=
CLANG=clang
CLANGXX=clang++
LLVM_AR=llvm-ar
LLVM_STRIP=llvm-strip
LLVM_RANLIB=llvm-ranlib
CLANG_INCLUDE_DIR=

CLANG_FORMAT=clang-format
CLANG_TIDY=clang-tidy
RUN_CLANG_TIDY=run-clang-tidy
SCAN_BUILD=scan-build

# GCC
GCC_VER=
GCC=gcc
GXX=g++
GCC_AR=gcc-ar
GCC_STRIP=strip
GCC_RANLIB=gcc-ranlib
GCC_INCLUDE_DIR=

# Choose toolchain for ONNX Runtime CMake subproject only
# --------------------------------------------------------------
# Options: 
#   - system|""  -> do not set CC/CXX (use /usr/bin/cc,c++)
#   - gcc        -> CC=GCC, CXX=GXX
#   - clang      -> CC=CLANG, CXX=CLANGXX
# --------------------------------------------------------------
ORT_TOOLCHAIN_FAMILY=system
ORT_CACHE_ROOT=~/.cache/idet/onnxruntime

# Cross build
# --------------------------------------------------------------
# TARGET_TRIPLE: gcc -dumpmachine OR clang -print-target-triple 
# SYSROOT: gcc --print-sysroot
# --------------------------------------------------------------
TARGET_TRIPLE=
SYSROOT=
