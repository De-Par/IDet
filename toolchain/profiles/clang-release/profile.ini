# =============
# clang-release
# =============

[profile]
desc = Clang release: portable release build, generic CPU, no sanitizers, no fast-math, no LTO

[build]
dir = build_clang_release
app_rel = src/app/idet/idet_app
