# ===========
# gcc-release
# ===========

[profile]
desc = GCC release: portable release build, generic CPU, no sanitizers, no fast-math, no LTO

[build]
dir = build_gcc_release
app_rel = src/app/idet/idet_app
